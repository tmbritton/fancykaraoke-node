---
import FooterQR from './FooterQR.astro';
interface Props {
	partySlug: string;
  showQR: boolean;
}

const { partySlug, showQR } = Astro.props;
---

<footer class="host-footer">
  <span class="partyId">Party Id: {partySlug}</span>
  <FooterQR slug={partySlug} showQR={showQR} />
</footer>

<style>
  .host-footer {
    display: flex;
    position: relative;
    justify-content: space-between;
    max-height: 2.5rem;
    background-color: var(--grey);
    padding: .5rem 2rem;
    position: fixed;
    bottom: 0;
    width: 100%;
  }
</style>
